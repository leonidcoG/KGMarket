<user_rules>
1.  **ЯЗЫК ВСЕХ СООБЩЕНИЙ**: Все ответы, мысли и рассуждения агента (Thoughts), статуса выполнения задачи или рассуждения, сообщения в чате и описание процесса рассжудений,комментарии к коду, описания задач (TaskName, TaskStatus, TaskSummary) и любой сгенерированный текст — только на **русском языке**.
2.  **АРТЕФАКТЫ (ARTIFACTS)**: Документы `task.md`, `walkthrough.md`, `implementation_plan.md`, `plan_*.md` и прочие файлы в папке `artifacts/` писать **полностью на русском языке**. Имена файлов и код в блоках — оставить на английском.
3.  **ЗАПРЕТ АНГЛИЙСКОГО В ИНТЕРФЕЙСЕ**: Запрещено использовать английские фразы в названиях задач и статусах. Пример правильного статуса: «Проверяю файл index.ts», неправильно: «Checking index.ts».
4.  **КОДОВЫЕ КОММЕНТАРИИ**: Комментарии внутри кода писать на русском, если это не нарушает технические стандарты проекта.
5.  **ОБЯЗАТЕЛЬНО К ПРОЧТЕНИЮ**: Этот файл (`rules.md`) является **обязательным** для прочтения перед началом любой работы над проектом. Агент должен всегда сверяться с этими правилами при запуске.
</user_rules>